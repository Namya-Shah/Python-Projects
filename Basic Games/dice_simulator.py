# importing random library to generate random number
import random

while True:
    rolled_num = random.randint(1,6)
    print("Rolled number is", rolled_num)
    input("Press any key to roll the dice again.")